filters:
  and:
    - file.inFolder("log/exercise")
    - "file.name != 'index'"
views:
  - type: table
    name: All
    order:
      - file.name
      - type
      - created
    sort:
      - property: created
        direction: DESC
